include ../components/pagination
include ../mixins/card
include ../mixins/banner

.section
	.container
		.row

			.col-lg-9.col-xl-8

				.main__space-col
					+card(card[0])
				.main__space-col
					+card(card[1])
				.main__space-col
					+card(card[4])

				.main__space-col
					+pagination(3, 1)(class='wrapper__pagination')

			.col-lg-3.col-xl-4
				.main__space-col
					+banner({
						link: '#'
					})
				.main__space-col
					+banner({
						link: '#'
					})
				.main__footer-spacer